# 页面标题
VITE_APP_TITLE = XFZL后台管理系统

# 测试环境配置
VITE_APP_ENV = 'staging'

# 测试环境后端地址（打包后浏览器直接访问，不要带 /stage-api 前缀）
VITE_APP_BASE_API = 'http://43.160.234.29:8080'

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip
