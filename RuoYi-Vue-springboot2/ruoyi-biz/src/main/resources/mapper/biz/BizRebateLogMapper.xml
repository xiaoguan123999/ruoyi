<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.BizRebateLogMapper">

    <insert id="insertRebateLog" parameterType="BizRebateLog" useGeneratedKeys="true" keyProperty="rebateId">
        insert into biz_rebate_log (order_id, member_id, currency, amount, rebate_date, create_time)
        values (#{orderId}, #{memberId}, #{currency}, #{amount}, #{rebateDate}, sysdate())
    </insert>

    <select id="sumAmountByMemberAndCurrency" resultType="java.math.BigDecimal">
        select ifnull(sum(amount), 0)
        from biz_rebate_log
        where member_id = #{memberId} and currency = #{currency}
    </select>
</mapper>
