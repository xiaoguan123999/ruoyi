<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.BizMemberLogininforMapper">

    <resultMap type="BizMemberLogininfor" id="BizMemberLogininforResult">
        <id     property="infoId"        column="info_id"/>
        <result property="memberId"      column="member_id"/>
        <result property="phone"         column="phone"/>
        <result property="status"        column="status"/>
        <result property="ipaddr"        column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser"       column="browser"/>
        <result property="os"            column="os"/>
        <result property="msg"           column="msg"/>
        <result property="loginTime"     column="login_time"/>
    </resultMap>

    <insert id="insertLogininfor" parameterType="BizMemberLogininfor" useGeneratedKeys="true" keyProperty="infoId">
        insert into biz_member_logininfor (member_id, phone, status, ipaddr, login_location, browser, os, msg, login_time)
        values (#{memberId}, #{phone}, #{status}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate())
    </insert>

    <select id="selectLogininforList" parameterType="BizMemberLogininfor" resultMap="BizMemberLogininforResult">
        select info_id, member_id, phone, status, ipaddr, login_location, browser, os, msg, login_time
        from biz_member_logininfor
        <where>
            <if test="memberId != null">and member_id = #{memberId}</if>
            <if test="phone != null and phone != ''">and phone like concat('%', #{phone}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
            <if test="ipaddr != null and ipaddr != ''">and ipaddr like concat('%', #{ipaddr}, '%')</if>
            <if test="params.beginTime != null and params.beginTime != ''">
                and login_time &gt;= #{params.beginTime}
            </if>
            <if test="params.endTime != null and params.endTime != ''">
                and login_time &lt;= #{params.endTime}
            </if>
        </where>
        order by info_id desc
    </select>

    <delete id="deleteLogininforByIds" parameterType="Long">
        delete from biz_member_logininfor where info_id in
        <foreach collection="array" item="infoId" open="(" separator="," close=")">
            #{infoId}
        </foreach>
    </delete>

    <update id="cleanLogininfor">
        truncate table biz_member_logininfor
    </update>
</mapper>
