<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.BizLotteryWinStrategyMapper">

    <resultMap type="BizLotteryWinStrategy" id="BizLotteryWinStrategyResult">
        <id     property="strategyId"      column="strategy_id"/>
        <result property="activityId"      column="activity_id"/>
        <result property="targetType"      column="target_type"/>
        <result property="userIds"         column="user_ids"/>
        <result property="packageId"       column="package_id"/>
        <result property="targetPrizeId"   column="target_prize_id"/>
        <result property="triggerCount"    column="trigger_count"/>
        <result property="loopMode"        column="loop_mode"/>
        <result property="stockMode"       column="stock_mode"/>
        <result property="status"          column="status"/>
        <result property="packageName"     column="package_name"/>
        <result property="createBy"        column="create_by"/>
        <result property="createTime"      column="create_time"/>
        <result property="updateBy"        column="update_by"/>
        <result property="updateTime"      column="update_time"/>
        <result property="remark"          column="remark"/>
    </resultMap>

    <select id="selectStrategyListByActivityId" parameterType="Long" resultMap="BizLotteryWinStrategyResult">
        select s.strategy_id, s.activity_id, s.target_type, s.user_ids, s.package_id,
               s.target_prize_id, s.trigger_count, s.loop_mode, s.stock_mode, s.status,
               p.package_name,
               s.create_by, s.create_time, s.update_by, s.update_time, s.remark
        from biz_lottery_win_strategy s
        left join biz_crowd_package p on p.package_id = s.package_id
        where s.activity_id = #{activityId}
        order by s.trigger_count asc, s.strategy_id asc
    </select>

    <select id="countByPackageId" parameterType="Long" resultType="int">
        select count(1) from biz_lottery_win_strategy where package_id = #{packageId}
    </select>

    <delete id="deleteStrategyByActivityId" parameterType="Long">
        delete from biz_lottery_win_strategy where activity_id = #{activityId}
    </delete>

    <insert id="insertStrategy" parameterType="BizLotteryWinStrategy" useGeneratedKeys="true" keyProperty="strategyId">
        insert into biz_lottery_win_strategy (
            activity_id, target_type, user_ids, package_id, target_prize_id,
            trigger_count, loop_mode, stock_mode, status, create_by, create_time, remark
        ) values (
            #{activityId}, #{targetType}, #{userIds}, #{packageId}, #{targetPrizeId},
            #{triggerCount}, #{loopMode}, #{stockMode}, #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateStrategy" parameterType="BizLotteryWinStrategy">
        update biz_lottery_win_strategy
        <set>
            <if test="targetType != null">target_type = #{targetType},</if>
            <if test="userIds != null">user_ids = #{userIds},</if>
            <if test="packageId != null">package_id = #{packageId},</if>
            <if test="targetPrizeId != null">target_prize_id = #{targetPrizeId},</if>
            <if test="triggerCount != null">trigger_count = #{triggerCount},</if>
            <if test="loopMode != null">loop_mode = #{loopMode},</if>
            <if test="stockMode != null">stock_mode = #{stockMode},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        where strategy_id = #{strategyId}
    </update>
</mapper>
