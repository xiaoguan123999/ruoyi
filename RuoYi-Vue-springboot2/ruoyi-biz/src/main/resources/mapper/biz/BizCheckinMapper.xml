<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.biz.mapper.BizCheckinMapper">

    <resultMap type="BizCheckin" id="BizCheckinResult">
        <id     property="checkinId"    column="checkin_id"/>
        <result property="memberId"     column="member_id"/>
        <result property="phone"        column="phone"/>
        <result property="checkinDate"  column="checkin_date"/>
        <result property="amount"       column="amount"/>
        <result property="currency"     column="currency"/>
        <result property="createTime"   column="create_time"/>
    </resultMap>

    <select id="selectByMemberAndDate" resultMap="BizCheckinResult">
        select checkin_id, member_id, checkin_date, amount, currency, create_time
        from biz_checkin
        where member_id = #{memberId} and checkin_date = #{checkinDate}
    </select>

    <select id="selectCheckinList" parameterType="BizCheckin" resultMap="BizCheckinResult">
        select c.checkin_id, c.member_id, m.phone, c.checkin_date, c.amount, c.currency, c.create_time
        from biz_checkin c
        left join biz_member m on m.member_id = c.member_id
        <where>
            <if test="memberId != null">and c.member_id = #{memberId}</if>
            <if test="phone != null and phone != ''">and m.phone like concat('%', #{phone}, '%')</if>
        </where>
        order by c.checkin_id desc
    </select>

    <insert id="insertCheckin" parameterType="BizCheckin" useGeneratedKeys="true" keyProperty="checkinId">
        insert into biz_checkin (member_id, checkin_date, amount, currency, create_time)
        values (#{memberId}, #{checkinDate}, #{amount}, #{currency}, sysdate())
    </insert>
</mapper>
